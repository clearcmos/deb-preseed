for file in /etc/secrets/* /etc/secrets/.*; do
    if [ -f "$file" ] && [ "$file" != "/etc/secrets/." ] && [ "$file" != "/etc/secrets/.." ] && [ "$file" != "/etc/secrets/.ignore" ]; then
        source "$file"
    fi
done
